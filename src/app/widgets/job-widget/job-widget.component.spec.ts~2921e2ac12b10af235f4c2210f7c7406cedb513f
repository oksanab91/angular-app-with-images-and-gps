import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { JobWidgetComponent } from './job-widget.component';

describe('JobWidgetComponent', () => {
  let component: JobWidgetComponent;
  let fixture: ComponentFixture<JobWidgetComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ JobWidgetComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(JobWidgetComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
